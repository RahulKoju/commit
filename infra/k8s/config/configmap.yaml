apiVersion: v1
kind: ConfigMap

metadata:
  name: commit-config
  namespace: commit

data:
  DB_HOST: commit-postgres
  DB_PORT: "5432"
  PORT: "8080"
  APP_ENV: production
  ALLOWED_ORIGINS: https://commit.rahulkoju.com.np,https://app.commit.rahulkoju.com.np
  COOKIE_DOMAIN: .rahulkoju.com.np
  JWT_EXPIRY_HOURS: "168"
  JWT_EXPIRY_MINUTES: "15"
  DB_MAX_CONNS: "10"
  DB_MIN_CONNS: "1"
  DB_MAX_CONN_LIFETIME_MINUTES: "60"
  DB_MAX_CONN_IDLE_MINUTES: "30"
  FOCUS_DAILY_MINIMUM_MINUTES: "120"
  EMAIL_FROM: contact@rahulkoju.com.np
  VITE_WEB_URL: https://commit.rahulkoju.com.np
