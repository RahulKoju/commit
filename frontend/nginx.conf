server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Check if the requested URI exists as a physical file or folder.
        # If it does not, fall back and serve index.html instead of a 404.
        try_files $uri $uri/ /index.html;
    }
}
